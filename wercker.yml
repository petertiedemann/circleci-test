box: microsoft/aspnetcore-build
build:
  steps:
    - script:
        name: echo
        code: |
          echo "hello world!"
    - script:
        name: dotnet
        code: |
          dotnet restore
          dotnet build
          dotnet test
