box: microsoft/aspnetcore-build
build:
  steps:
    - script:
        name: echo
        code: |
          echo "hello world!"
    - script:
        name: dotnet build
        code: |
          dotnet restore
          dotnet build app
          dotnet build test
          dotnet test
