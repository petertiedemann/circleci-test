box: microsoft/aspnetcore-build
build:
  steps:
    - script:
        name: dotnet build
        code: |
          dotnet restore
          dotnet build app
          dotnet build test
          dotnet test test
